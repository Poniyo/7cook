<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rayola.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".TestDebugActivity" />
        <activity
            android:name=".ProfileActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".LastNewsActivity" />
        <activity android:name=".OneFragmentActivty" />
        <activity
            android:name=".ExpertProfileActivity"
            android:label="@string/app_name" />
        <activity android:name=".viewpager.ViewPagerSampleActivity" />
        <activity android:name=".dialogGh10.DialogSampleActivity" />
        <activity android:name=".multitheredinggh11.MainTheredActivity" />
        <activity android:name=".weatherapp.MainWeatherAppActivity"></activity>
        <activity android:name=".cookAppGhesmat13.CookMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>